var prompt = prompt('request');
var Word = require('./word.js')

//call the .start method on prompt
game.startGame();

var game = function(game) {
	wordBank: ['sunshine', 'beach', 'volleyball'];
	guessesRemaining: 10;
	currentWrd: null; 
    this.word = wrd;

var zbtWord = new Word('sunshine');
currentWrd = zbtWord;

.getLets function(currentWrd) {

	keepPromptingUser() {	
	  self = keepPromptingUser;
	}
}

client.get('statuses/user_timeline', params, function(error, tweets, response){
  if (!error) {
    console.log(tweets);
  }
});


prompt.get('u', 'h', 's','n', 'i', 'e', );  {
	guessLetter =['i']

	anonymous function(err, result)) {
		if (!error) {
    console.log("The Letter or space you guessed is " + result.guessed)
	}
}

findHowManyOfUserGuess = currentWrd.checkIfLetterFound(result.guessLetter);


if findHowManyOfUserGuess = 0 {
		console.log("You guessed wrong~!" - guessesRemaining)
	} else {
		console.log("You guessed right")
		if function currentWrd.didWeFindTheWord() returns true {
			console.log('You Won!!!')
			return 1 to the end of the game
			console.log("guessesRemaining: " + value of guessesRemaining);
			console.log("call the wordRender() mthod on the currentWrd ")
			}
		}
	}

	if (guessesRemaining > 0 && currentWrd.found == 'false') {
	keepPromptingUser()
	}else{
		if the guessesRemaining = 0 {
			console.log("Game over bro", this.Word)
		}else{
			console.log(wordRender(currentWrd))
		}
	}
game.startGame();




module.exports = Letter;